// Attempt to update first element
method update1st(&(int[]) list, int value):
    // First, check whether list is empty or not
    if (*list) != [0;0]:
       // Then, update 1st element
       (*list)[0] = value
    // done
